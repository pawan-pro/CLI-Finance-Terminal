# Context Snapshot: Quantwater Terminal Build (Hackathon)
**Current Date:** January 21, 2026  
**Objective:** Build an institutional-grade **Web-based AI Dashboard (Nexus Terminal)** that analyzes proprietary market data, bond yields, and sectoral flows using a FastAPI/DuckDB backend and Google Gemini 1.5 Flash.

## 1. Core Architecture (The Data Pipeline)
*   **Producers (Multi-Source):** 
    *   **MetaTrader 5 (MT5):** Exports M15 bar data for 50+ individual symbols. 
    *   **Twelve Data (T12):** Provides high-frequency (M15) history for **Bond Proxies** (SHY, IEF, TLT) and **S&P 500 Sectors**.
    *   **Synthetic Engine:** Calculates the **US Dollar Index ($DXY)** and **Treasury Yield Estimates** using calibrated Duration-based mathematical models.
*   **Bridge Layer (FastAPI):** `app.py` runs on port 8000, serving as the "Nervous System." It bridges the DuckDB Silver Layer to the React Frontend via JSON endpoints (`/api/market`, `/api/calendar`, `/api/research`).
*   **Frontend (Nexus Terminal):** A professional React/TypeScript UI (built in AI Studio) that features real-time watchlists, macro dashboards, and an integrated AI chat sidebar.
*   **Bridge & Pathing:** Enforced **Forced Absolute Pathing** across all scripts to point to the master DuckDB file at `/Users/pawan/CLI-Finance-Terminal/approach 3.0/Hackathon/data/silver/market_data.duckdb`.

## 2. Repository Directory Structure
```text
approach 3.0/Hackathon/
├── data/
│   ├── bronze/                  # Raw CSVs from MT5
│   ├── silver/
│   │   └── market_data.duckdb   # Master Data Lake (Central Absolute Source)
├── nexus-terminal/
│   └── nexus-terminal/          # React Frontend Source
│       ├── src/
│       │   ├── App.tsx          # Main Dashboard Logic & State
│       │   ├── components/      # Auth, Charts, Macro Views
│       │   └── services/        # API Service Layer (Wiring to port 8000)
├── src/
│   ├── ingestion/
│   │   ├── fetch_high_freq.py   # T12 Ingester (Bonds + Sectors)
│   │   └── ingest_calendar.py   # Hardened Macro Ingester (Deduplication Logic)
│   ├── analytics/
│   │   └── metrics_engine.py    # Yield Calibration + Name Sanitizer
├── app.py                       # FastAPI Web Bridge (Backend -> UI)
├── quantwater.py                # Legacy CLI for data validation
└── .env                         # API Keys (Gemini & TwelveData)
```

## 3. Current Outcome & Key Logic
*   **Web Transition:** Successfully ported the terminal from CLI to a professional React UI. Established local API connectivity to allow the browser to see "Ground Truth" institutional data.
*   **Calibrated Yield Model:** Implemented a **Modified Duration Formula** in the backend to estimate real-time yields from ETF prices (e.g., US10Y @ 4.40%).
*   **Institutional UX:** Deployed a **Name Sanitization Layer** on the API level, ensuring the React UI displays "10Y Treasury" and "Gold Spot" instead of technical symbol strings.
*   **Deduplicated Macro Feed:** Hardened the calendar ingestion to handle overlapping MT5 exports without `ConstraintError` crashes.

## 4. Immediate Goals (Jan 22 Session)
*   **Mathematical Precision:** Refine the US30Y Duration constant (Target: 18.2) to bring estimates below 5%. Implement a `COALESCE` fallback to resolve the $DXY `NaN` history error.
*   **UI Integration Finalization:** Use Qwen CLI to complete the refactor of `marketDataService.ts` and `App.tsx` to ensure every visual component (Watchlist, Ticker, Chart) is powered by the FastAPI `/api/market` feed.
*   **Causal AI Synthesis:** Verify the Gemini Assistant in the Web UI correctly references local data and Bond/Equity correlations in its responses.

## 5. Key Endpoints
*   `GET /api/market`: Full institutional market snapshot (Prices/Yields/24h Returns).
*   `GET /api/calendar`: High-impact economic triggers.
*   `GET /api/research`: AI-synthesized macro briefing.

---
**Status:** The "Integration Phase" is 75% complete. The backend is stable and calibrated; the focus is now on the final "wiring" of the React components to deliver a production-ready institutional terminal.

**End of Context Block.**